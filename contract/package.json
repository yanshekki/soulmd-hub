{
  "name": "soulmd-agentfi-contract",
  "version": "1.0.0",
  "description": "NEAR Smart Contract for SoulMD Hub AgentFi",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "near-sdk-js build src/contract.ts build/contract.wasm",
    "deploy": "NEAR_ENV=mainnet near deploy soulmd-hub.near build/contract.wasm --networkId mainnet",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "near-sdk-js": "^2.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}