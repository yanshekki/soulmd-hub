<?php
/**
 * SoulMD Hub - Grand Unified Documentation Sub-module
 * Target: /docs/intro
 * Layout: Enterprise Modular Architecture & Stateless Routing Matrix
 * 🚀 Patched: Removed 100% of third-party LLM brand references & Expanded into a deep unredacted enterprise layout.
 */

// 確保此檔案不被直接存取，必須由 docs.php 引入
if (!defined('BASE_URL')) {
    header("HTTP/1.0 404 Not Found");
    exit;
}
?>

<div class="space-y-12 animate-fade-in">
    
    <div class="border-b border-white/10 pb-6">
        <h2 class="text-2xl sm:text-3xl font-black text-white tracking-tight mb-4">
            <?= __('Ecosystem Architectural Blueprint') ?>
        </h2>
        <p class="text-zinc-400 text-sm sm:text-base leading-relaxed max-w-5xl">
            <?= __('Ecosystem Blueprint Core Intro: SoulMD Hub establishes a professional, zero-overhead, multi-tenant framework engineered to handle granular prompt trees securely. By abstracting the interaction layer between clients and cognitive logic models, the core ecosystem decouples model rendering, persistent caching, and cryptographic user state tracking without relying on stateful persistent socket overheads.') ?>
        </p>
    </div>

    <div class="bg-zinc-950/40 border border-white/5 rounded-3xl p-6 sm:p-8 md:p-10 shadow-inner hover:border-white/10 transition-all duration-300">
        <h3 class="text-xl sm:text-2xl font-bold text-emerald-400 mb-4 flex items-center gap-3 select-none">
            <i class="fas fa-server bg-emerald-500/10 p-2.5 rounded-xl border border-emerald-500/20 text-base sm:text-lg"></i>
            <?= __('Infrastructure Isolation Firewall') ?>
        </h3>
        <p class="text-sm text-zinc-400 mb-8 leading-relaxed">
            <?= __('Decoupled Architecture Matrix: Traditional multi-modal systems suffer from intense memory locks and process degradation when handling multi-file JSON trees. SoulMD Hub eliminates framework bloat by employing native vanilla processing, shifting the computational weights to programmatic object models while enforcing rigorous cross-origin runtime shielding.') ?>
        </p>
        
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div class="bg-zinc-900/40 border border-white/5 p-6 rounded-2xl shadow-md hover:border-emerald-500/20 transition-colors">
                <h4 class="text-white font-bold text-base mb-3 flex items-center gap-2">
                    <i class="fas fa-microchip text-emerald-400 text-xs"></i> <?= __('Bare-Metal Compiling Core') ?>
                </h4>
                <p class="text-xs sm:text-sm text-zinc-400 leading-relaxed">
                    <?= __('Bare-metal execution pattern handles user data transformations seamlessly. Memory structures are optimized at the execution level, bypassing resource heavy framework runtimes. This guarantees near-zero latency overhead when executing custom context injection matrices and complex sliding summarization routines.') ?>
                </p>
            </div>
            <div class="bg-zinc-900/40 border border-white/5 p-6 rounded-2xl shadow-md hover:border-emerald-500/20 transition-colors">
                <h4 class="text-white font-bold text-base mb-3 flex items-center gap-2">
                    <i class="fas fa-shield-alt text-emerald-400 text-xs"></i> <?= __('Headless Cross-Origin Perimeter') ?>
                </h4>
                <p class="text-xs sm:text-sm text-zinc-400 leading-relaxed">
                    <?= __('Headless security walls permanently lock mutated processing paths. By isolating CORS pre-flight configurations and strictly rejecting multipart form fallback queries, mutations are restricted to pure application/json streams, rendering standard request-forgery templates completely mathematically inert.') ?>
                </p>
            </div>
        </div>
    </div>

    <div class="bg-zinc-950/40 border border-white/5 rounded-3xl p-6 sm:p-8 md:p-10 shadow-inner hover:border-white/10 transition-all duration-300">
        <h3 class="text-xl sm:text-2xl font-bold text-purple-400 mb-4 flex items-center gap-3 select-none">
            <i class="fas fa-cubes bg-purple-500/10 p-2.5 rounded-xl border border-purple-500/20 text-base sm:text-lg"></i>
            <?= __('Cascading Semantic Compiler') ?>
        </h3>
        <p class="text-sm text-zinc-400 mb-8 leading-relaxed">
            <?= __('Prompt Modularization Engine: Standard single-file prompts degrade rapidly as token history expands, shifting character weights and leaking structural bounds. SoulMD Hub enforces an unredacted multi-file segmentation process that dynamically parses prompt files into a consolidated operational memory context at the threshold level:') ?>
        </p>
        
        <div class="space-y-4">
            <div class="bg-zinc-900/50 border border-emerald-500/10 p-6 rounded-2xl relative overflow-hidden group hover:border-emerald-500/30 transition-all">
                <div class="absolute top-0 left-0 w-1 h-full bg-emerald-500"></div>
                <div class="flex flex-col sm:flex-row sm:items-center justify-between gap-2 mb-3">
                    <h4 class="text-white font-bold text-base flex items-center gap-2">
                        <i class="fas fa-brain text-emerald-400"></i> <?= __('SOUL.md Foundational Layer') ?>
                    </h4>
                    <span class="text-[10px] font-mono text-zinc-500 uppercase tracking-widest bg-zinc-950 px-2 py-0.5 rounded border border-white/5 select-none">Priority 1 (Base Identity)</span>
                </div>
                <p class="text-xs sm:text-sm text-zinc-400 leading-relaxed">
                    <?= __('SOUL.md functions as the authoritative core structure of the architecture. It maps system behavioral parameters, environmental contexts, specific knowledge graphs, and default identity matrices. This block serves as the unalterable system foundation injected natively at the prefix tier of every cognitive request.') ?>
                </p>
            </div>

            <div class="bg-zinc-900/50 border border-purple-500/10 p-6 rounded-2xl relative overflow-hidden group hover:border-purple-400/30 transition-all">
                <div class="absolute top-0 left-0 w-1 h-full bg-purple-500"></div>
                <div class="flex flex-col sm:flex-row sm:items-center justify-between gap-2 mb-3">
                    <h4 class="text-white font-bold text-base flex items-center gap-2">
                        <i class="fas fa-palette text-purple-400"></i> <?= __('STYLE.md Acoustic & Linguistic Modifiers') ?>
                    </h4>
                    <span class="text-[10px] font-mono text-zinc-500 uppercase tracking-widest bg-zinc-950 px-2 py-0.5 rounded border border-white/5 select-none">Priority 2 (Voice Matrix)</span>
                </div>
                <p class="text-xs sm:text-sm text-zinc-400 leading-relaxed">
                    <?= __('STYLE.md controls the acoustic variables and lexical distributions of the output generation layer. It dynamically configures vocabulary weights, response density metrics, markdown formatting structures, and syntax trees. This enforces strict linguistic filtering, ensuring output style remains unredacted across long execution logs.') ?>
                </p>
            </div>

            <div class="bg-zinc-900/50 border border-red-500/10 p-6 rounded-2xl relative overflow-hidden group hover:border-red-400/30 transition-all">
                <div class="absolute top-0 left-0 w-1 h-full bg-red-500"></div>
                <div class="flex flex-col sm:flex-row sm:items-center justify-between gap-2 mb-3">
                    <h4 class="text-white font-bold text-base flex items-center gap-2">
                        <i class="fas fa-exclamation-triangle text-red-400"></i> <?= __('RULES.md Negative Rails & Security Boundaries') ?>
                    </h4>
                    <span class="text-[10px] font-mono text-zinc-500 uppercase tracking-widest bg-zinc-950 px-2 py-0.5 rounded border border-white/5 select-none">Priority 3 (Constraint Firewall)</span>
                </div>
                <p class="text-xs sm:text-sm text-zinc-400 leading-relaxed">
                    <?= __('RULES.md constructs unbreakable compliance rails and strict hallucination safeguards. By establishing a rigid list of negative constraints, character escape restrictions, and output boundaries, this module dynamically overrides volatile token states, ensuring absolute functional alignment and safety.') ?>
                </p>
            </div>
        </div>
    </div>

    <div class="bg-zinc-950/40 border border-white/5 rounded-3xl p-6 sm:p-8 md:p-10 shadow-inner hover:border-white/10 transition-all duration-300">
        <h3 class="text-xl sm:text-2xl font-bold text-blue-400 mb-4 flex items-center gap-3 select-none">
            <i class="fas fa-exchange-alt bg-blue-500/10 p-2.5 rounded-xl border border-blue-500/20 text-base sm:text-lg"></i>
            <?= __('Dual-Track Decoupled Gateway') ?>
        </h3>
        <p class="text-sm text-zinc-400 mb-8 leading-relaxed">
            <?= __('Stateless Gateway Routing: To guarantee uncompressed operational throughput, the processing engine intercept inputs instantly, splitting structural processing workflows into localized text processing components or multimodal vision processing arrays based on programmatic data validation layers.') ?>
        </p>
        
        <div class="space-y-4">
            <div class="flex flex-col sm:flex-row items-start gap-4 sm:gap-6 bg-zinc-900/40 border border-white/5 p-6 rounded-2xl shadow-md hover:border-blue-500/20 transition-colors">
                <div class="w-12 h-12 bg-blue-500/10 border border-blue-500/20 rounded-xl flex items-center justify-center text-blue-400 shrink-0 shadow-lg shadow-blue-500/5">
                    <i class="fas fa-font text-xl"></i>
                </div>
                <div class="space-y-2 flex-1 min-w-0">
                    <h4 class="text-white font-bold text-base"><?= __('High-Performance Text Reasoning Engine Cluster') ?></h4>
                    <p class="text-xs sm:text-sm text-zinc-400 leading-relaxed">
                        <?= __('Text logical processing pathway handles structural natural language queries natively. Low-tier configurations trigger extreme velocity flash token clusters, while advanced accounts escalate queries to dense logic-thinking matrices, providing comprehensive internal thoughts processing maps before returning text strings.') ?>
                    </p>
                </div>
            </div>
            
            <div class="flex flex-col sm:flex-row items-start gap-4 sm:gap-6 bg-zinc-900/40 border border-white/5 p-6 rounded-2xl shadow-md hover:border-blue-500/20 transition-colors">
                <div class="w-12 h-12 bg-amber-500/10 border border-amber-500/20 rounded-xl flex items-center justify-center text-amber-400 shrink-0 shadow-lg shadow-amber-500/5">
                    <i class="fas fa-images text-xl"></i>
                </div>
                <div class="space-y-2 flex-1 min-w-0">
                    <h4 class="text-white font-bold text-base"><?= __('Multimodal Serverless Vision Grid') ?></h4>
                    <p class="text-xs sm:text-sm text-zinc-400 leading-relaxed">
                        <?= __('Multimodal data analysis intercepts binary packages and base64 structures asynchronously. Requests are streamed across specialized multi-modal nodes trained for dense chart calculations, system blueprint decoding, layout OCR mapping, and spatial grid processing, optimizing context limits under 15 seconds.') ?>
                    </p>
                </div>
            </div>
        </div>
    </div>

</div>